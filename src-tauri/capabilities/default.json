{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the receipt input app (desktop + mobile).",
  "platforms": ["linux", "macOS", "windows", "android", "iOS"],
  "permissions": [
    "core:default",
    "os:default",
    "dialog:default",
    "dialog:allow-save"
  ]
}
